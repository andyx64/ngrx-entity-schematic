{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "ng-add": {
      "description": "Generates all NgRx Entity files for an application",
      "factory": "./ngrx-firestore/index",
      "schema": "./ngrx-firestore/schema.json"
    },
    "add": {
      "description": "Generates all NgRx Entity files for an application",
      "factory": "./ngrx-firestore/index",
      "schema": "./ngrx-firestore/schema.json"
    }
  }
}
